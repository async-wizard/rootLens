package com.rootlens.incidentengine.config;

import org.springframework.context.annotation.Configuration;

// StringRedisTemplate is auto-configured by Spring Boot when spring-boot-starter-data-redis
// is on the classpath and spring.data.redis.host/port are set in application.yml.
// No additional bean definitions are needed for MVP.
@Configuration
public class RedisConfig {
}
