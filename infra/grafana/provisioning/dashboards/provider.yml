apiVersion: 1

providers:
  - name: rootLens
    orgId: 1
    folder: rootLens
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboard-files
